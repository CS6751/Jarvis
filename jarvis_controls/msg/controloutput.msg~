#Publish the output motor commands -- could be useful for high level control to verify nothing really bad is happening.

Header header

#Wheels will be velocity controlled. This will spit out the velocities commanded.
float64 LFwheelVel
float64 RFwheelVel
float64 LRwheelVel
float64 RRwheelVel

#Arm joings will be current controlled.

float64 J1current
float64 J2current
float64 J3current
float64 J4current
float64 J5current
